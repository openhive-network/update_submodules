Alternate take on the script, generated by ChatGPT instead of CoPilot.

Only tested on toy repositories, auto merge functionality not tested and may have syntax errors.

```
apt install python3-git python3-yaml python3-ruamel.yaml
```
or
```
pip install GitPython PyYAML ruamel.yaml
```
